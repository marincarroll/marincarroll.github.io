---
import ContactInfo from "./ContactInfo.astro";
import { basics } from "@resume";
const { summary } = basics;
---

<section class="profile">
  <!--<h2 class="profile__title">-->
  <!--  {label}-->
  <!--  <span aria-hidden="true">/</span>-->
  <!--  {city}, {region}-->
  <!--</h2>-->
  <p class="profile__summary">{summary}</p>
  <!--<p class="profile__skills">{skills}</p>-->
  <ContactInfo />
</section>

<style>
  .profile {
    max-width: 70ch;
  }

  @media screen {
    .profile {
      position: sticky;
      top: 0;
      padding-block: calc(var(--header-height) + (1.5 * var(--border-radius)))
        4em;
      z-index: -1;
    }
  }

  @media print {
    .profile {
      padding-block-end: 3em;
    }
  }

  .profile__summary {
    font-size: 1.2em;
      font-weight: 500;
      margin-block: 0 1em;
  }

 <!--.profile__skills {-->
 <!--    text-transform: uppercase;-->
 <!--    font-weight: 600;-->
 <!--    letter-spacing: 0.07em;-->
 <!--    -webkit-font-smoothing: antialiased;-->
 <!--}-->
</style>
